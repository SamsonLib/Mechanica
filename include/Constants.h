#pragma once

namespace Constants {
    const int BLOCK_SIZE = 128;        // Pixels per block
    const int CHUNK_SIZE = 16;        // Blocks per chunk dimension
    const int SCREEN_WIDTH = 1280;
    const int SCREEN_HEIGHT = 720;
    const float SPEED = 600.0f;
    const int TARGET_FPS = 180;
    const char* WINDOW_TITLE = "Mechanica";
    const char* WORLD_FILE = "world.dat";
    const int SIDEBAR_WIDTH = 80;
    const int CURSOR_OUTLINE_THICKNESS = 3;
    const int FONT_SIZE = 5;
} // namespace Constants

